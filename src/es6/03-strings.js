// como se hacia antes
let hello = 'Hello';
let world = 'World';
let epicPhrase = hello + ' ' + world + '!';
// console.log(epicPhrase);

// template literal
let epicPhrase2 =  `${hello} ${world}!`;
console.log(epicPhrase2);

// multi-line strings
let lorem = 'esto es un string \n' + 'esto es otra linea';

let lorem2 = `Esta es un frase épica
la continuación de la frase épica.`;
console.log(lorem);
console.log(lorem2);
